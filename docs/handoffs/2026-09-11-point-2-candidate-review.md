# Adversarial review of candidate-fix.patch — CHANGES REQUIRED

1. **P0 parent-order deadlocks:** existing `set_staff_password`, `set_manager_status`, and `decide_manager_reset` lock/update manager (and reset child) before calling the newly parent-first `revoke_manager_sessions`, creating manager→restaurant vs restaurant-delete→manager cycles. Forward-replace every manager mutation caller: unlocked ID discovery, restaurant parent lock, manager lock, manager advisory lock, re-read authorization/state, mutate, revoke in canonical order. Add deterministic restaurant-delete races.
2. **P0 legacy invalid overlap/cutover:** registry backfill does not repair unconfirmed pending hashes already overlapping active or manager tombstones; exact retry bypasses checks; old lifecycle writes can race the migration backfill. Lock lifecycle tables during cutover, detect/terminally tombstone invalid overlaps (excluding normal confirmed-pending+active), reject overlaps in exact retry and confirmation, and test seeded legacy states before R11 apply.
3. **P1 browser recovery:** pending-record persistence failure is ignored. If identity write succeeds but pending record fails/corrupts, fresh login can send new pending token as old and self-revoke. Treat persistence failure as hard pre-confirm failure with exact cleanup; add server equality guard so old token equal to newly minted pending is never revoked. On uncertain failure after pre-confirm navigation, return/navigate to login or let manager route resume; do not rely on storage for mandatory revocation correctness.
4. **P1 stale identity:** definitive failures after identity write (navigation failure, successful cleanup, authoritative FAILED) leave raw pending manager identity. Remove newly written identity on every definitive failure. Keep identity+recovery only for genuinely unknown/unresolved state. Test storage absence.
5. **P1 exact reconciliation after confirmed cascade/retention:** active tombstone alone lacks reservation identity, so exact pair can become forever UNKNOWN after manager/restaurant deletion or confirmed-row/reservation retention. Permanent registry must include reservation ID, manager ID, and authoritative lifecycle state, populated at mint and updated on confirmation/deletion. Reconciliation should use exact durable evidence. Test active revoke, parent cascades, and retention.
6. **P2 cleanup throughput:** one-manager-per-call expiry/retention lacks operational loop. Add bounded globally safe loop or scheduler-oriented batching and multi-manager tests.
7. **Tests:** replace sleeps with observed backend wait states/locks; assert exact winner/verdict/final rows. Add real unmount/navigation recovery behavior, partial storage write, equal-token guard, registry RLS/table privileges/search_path, and upgrade/cutover tests.
8. Areas accepted: strict manager logout with rejected-promise normalization; comments; no raw token in SQL/UI/log found; function search_path and intended grants looked sound; fail-closed non-destructive tombstone cleanup.
